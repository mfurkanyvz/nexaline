# NexaLine ASİSTAN yayın planı

Yeni statik servis: `nexaline-asistan`

Alan adları:

- `nexalineapp.xyz`
- `www.nexalineapp.xyz`

Eski ChatApp/sohbet servisi yalnızca şu alan adlarında kalır:

- `nidar.com.tr`
- `www.nidar.com.tr`

Render üzerinde önce yeni servis yayınlanır. Ardından `nexalineapp.xyz` ve
`www.nexalineapp.xyz` eski `nexaline` servisinin Custom Domains bölümünden
çıkarılıp yeni `nexaline-asistan` servisine eklenir. NIDAR alan adlarına ve
veritabanına dokunulmaz.
